 
        # self.Pushwifi.clicked.connect(self.Push_wifi)
    #     import serial.tools.list_ports
    #     for i in serial.tools.list_ports.comports():
    #         port = str(i).split(" ")[0]
    #         self.Getport.addItem(port)
        
        
    #     self.Getstt.clicked.connect(self.SelectPort)
           
        self.show()
    # def SelectPort(self):
    #     print(self.Getport.currentText())
    #     port = self.Getport.currentText()
    def Push_wifi(self):
        if checkconnect.flag == True:
            self.Led.setStyleSheet('background-color: rgb(0:0:0)')
            self.Connectstt.setText('as')